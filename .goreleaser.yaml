builds:
  - binary: pulumi-component-aws-ecs
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags: -s -w
    env:
      - CGO_ENABLED=0
checksum:
  name_template: '{{ .ProjectName }}_{{ .Tag }}_checksums.txt'